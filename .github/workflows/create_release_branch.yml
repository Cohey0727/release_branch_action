name: Create Release Branch
on:
  workflow_dispatch:
    inputs:
      VersionUpType:
        description: "Version Up Type"
        default: Minor
        type: choice
        required: true
        options:
          - Major
          - Minor
          - Patch

jobs:
  createrelease:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v2
      # - name: Increment Version Tag
      #     run: |
      #       #!/bin/bash
      #       set -e

      #       # Find and increment the version number.
      #       perl -i -pe 's/^(version:\s+\d+\.\d+\.\d+\+)(\d+)$/$1.($2+1)/e' version.txt

      #       # Commit and tag this change.
      #       version=`grep 'version: ' version.txt | sed 's/version: //'`
      #       git add version.txt
      #       git commit -m "Bump version to $version" version.txt
      #       git tag $version
